# Request Def
[request_definition]
r = sub, obj, act

# Policy Def
[policy_definition]
p = sub, obj, act

# Polict Effect
[policy_effect]
e = some(where (p.eft == allow))

# Matchers
[matchers]
m= r.sub == p.sub && r.obj == p.obj && r.act == p.act
